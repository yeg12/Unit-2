// Write an if/else that checks if a number is even or odd (hint: use the % operator — num % 2 === 0).
// Write a function canRide(height) that prints "You can ride!" if height is 48 inches or more, and "Sorry, too short" otherwise.
// Write a switch statement that takes a season variable ("winter", "spring", "summer", "fall") and prints a typical activity for each season.
// Combine && and ||: write a condition for a movie ticket discount that applies if the person is a student or a senior, and it's a weekday.

function ODD_EVEN(X) {
    if (X % 2 === 0) {
        console.log("TRUE")
    } else {
        console.log("FALSE")
    }
}

ODD_EVEN(2)


function canride(V) {
    If (V >= 18)
}